# 分析機能 実装計画

**状態: 未実装（計画のみ）**
最終更新: 2026-08-11

この文書は、メニューの「分析」で提供する集計機能の計画です。

> **旧計画について**
> `編集報告システム_仕様書_完全版 (1).md` の「⑩ ダッシュボード画面」にも
> 分析の記載がありますが、あれは **Google Apps Script 時代のもの**で、
> 現在のデータ構造・権限・承認フローと噛み合いません。
> 分析機能についてはこの文書を正とします。

---

## 1. 前提となる現在のデータ

集計に使える実データは以下です。**ここに無いものは集計できません。**

### projects（案件）

| カラム | 内容 | 集計での用途 |
|---|---|---|
| `client_id` | クライアント | クライアント別集計 |
| `genres` / `technologies` | ジャンル・使用技術（配列） | ジャンル別分布 |
| `main_editor` / `sub_editors` / `director` | 担当者 | 担当者別集計 |
| `status` | 進捗 | ステータス別件数 |
| `registration_date` / `delivery_date` | 登録日・納品予定日 | 期間の軸 |
| `actual_delivery_date` | 実納品日 | 納期遵守率 |
| `submitted_at` / `leader_approved_at` | 申請日時・承認日時 | リードタイム |
| `target_hours` | 編集目標時間（**分単位**） | 目標 vs 実績 |
| `estimate_total` | 編集費合計（内訳の自動合計） | 売上（編集費） |
| `estimate_grand_total` | 見積合計（編集費以外を含む） | 売上（全体） |

### edit_history（編集時間）

| カラム | 内容 |
|---|---|
| `project_id` / `editor` / `date` | 案件・編集者・作業日 |
| `edit_time` | 作業時間（**分単位**） |
| `estimate_item` | 作業項目（`estimate_items` への参照） |

### マスタ

- `edit_items` … 見積内訳の作業項目。`hourly_rate` あり
- `estimate_items` … 編集時間登録の作業項目。`hourly_rate` あり

### ⚠️ 実装前に把握しておくべき制約

**(a) 作業項目マスタが2つに分かれています**

- 見積内訳 `estimate_breakdown.edit_item_id` → **`edit_items`**
- 編集時間 `edit_history.estimate_item` → **`estimate_items`**

同じ「作業項目」の概念に別テーブルが使われているため、
**「項目ごとの見積 vs 実績」は現状のままでは突き合わせられません。**
項目別の分析をやるなら、事前にマスタの統合が必要です。
案件単位の集計（目標時間 vs 実働時間の合計）は問題なくできます。

**(b) `executive_approved` には到達しません**

承認はリーダー1段階で完結する運用のため、このステータスは常に0件です。
ステータス別のグラフに含めても意味がありません。

**(c) 原価データがありません**

編集者ごとの人件費単価を持っていないため、正確な利益は算出できません。
下記「利益系の指標」を参照してください。

**(d) part_time は自分が関わる案件しか見えません**

RLS により、案件関係者以外のデータは取得できません（リーダー・役員は全件可）。
集計結果も権限によって変わります。**全社集計はリーダー以上に限定**するのが自然です。

---

## 2. 実装の優先順位

既存データだけで作れて、定義の議論が要らないものから着手します。

### フェーズ1：既存データのみで作れるもの

**1-A. 自分の編集時間集計（全権限）**

- 今月・今週の編集時間合計
- 案件別の編集時間（上位5件）
- 直近3ヶ月の月別推移

`edit_history` を `editor = 自分` で集計するだけです。
アルバイトのダッシュボードは現在ほぼ空なので、ここが最初の価値になります。

**1-B. 目標時間 vs 実働時間（staff以上）**

- 案件ごとの `target_hours` と `edit_history` 合計の比較
- 超過している案件のリスト（超過時間の大きい順）
- 全体の平均超過率

案件詳細画面に既にある計算をそのまま一覧化するもので、実装コストが低いです。

**1-C. 案件ステータス別件数（staff以上）**

下書き / 申請中 / 差戻し / リーダー承認済 の4分類。
`executive_approved` は含めません。

### フェーズ2：期間軸を持つもの

**2-A. 月別案件数の推移（直近6ヶ月）**
`registration_date` または `delivery_date` を軸にした件数。どちらを使うかは要確認。

**2-B. 編集者別の実績**
案件数・編集時間の合計。サブ編集者としての参加も数えるかは要確認。

**2-C. クライアント別の実績**
案件数・売上（`estimate_grand_total` の合計）。

**2-D. ジャンル別の案件分布**
`genres` は配列のため、1案件が複数ジャンルに計上される点に注意。

**2-E. 納期遵守率**
`actual_delivery_date <= delivery_date` の割合。

### フェーズ3：定義を決めてから

**3-A. 利益系の指標**

売上は `estimate_grand_total`（または `estimate_total`）で取れますが、
**原価の定義が決まっていません。** 候補は以下です。

| 案 | 原価の定義 | 長所 | 短所 |
|---|---|---|---|
| A | 実働時間 × 作業項目の `hourly_rate` | 既存データだけで算出できる | 見積単価であり実際の人件費ではない |
| B | 実働時間 × 編集者ごとの時給 | 実態に近い | `users` に時給カラムの追加が必要 |
| C | 実働時間 × 権限別の固定単価 | 追加は設定値のみで済む | 精度は粗い |

**まず案Aで「見積との差」として出し、必要になったら案Bへ移行**するのが
現実的だと考えています。案Aは厳密には利益ではないため、
画面上は「利益率」ではなく **「見積時間との差」「消化率」** といった
誤解のない名称にすべきです。

---

## 3. 権限ごとの表示範囲

| 権限 | 見せる範囲 |
|---|---|
| part_time（アルバイト） | 自分の編集時間のみ（1-A） |
| staff（編集者） | 自分の編集時間 ＋ 自分が関わる案件の目標vs実績 |
| leader（リーダー） | 全社集計 |
| executive（役員） | 全社集計 |

RLS が下支えするため、フロントで範囲を誤っても他人のデータは取得できません。
ただし**画面に出す項目は権限ごとに分けるべき**です。
アルバイトに全社の売上を見せる必要はありません。

---

## 4. 技術的な検討事項

**グラフライブラリが未導入です**

現在 `index.html` が読み込む外部スクリプトは supabase-js のみです。
グラフを描くなら Chart.js などの追加が必要になります。
フェーズ1は数値とテーブルだけで成立するため、**ライブラリ導入はフェーズ2から**で構いません。

**集計をどこで行うか**

| 方式 | 向き | 備考 |
|---|---|---|
| フロントで集計 | フェーズ1 | 実装が速い。件数が増えると転送量が問題になる |
| Postgres のビュー | フェーズ2以降 | RLS が効くので安全。SQLの管理が増える |
| RPC 関数 | 複雑な集計 | `SECURITY DEFINER` にすると RLS を迂回するため要注意 |

まずフロント集計で作り、遅くなってから移すのが妥当です。
20名・年間数百件の規模であれば、当面はフロント集計で足ります。

**自動更新について**

旧計画にあった「5分ごとの自動更新」は不要と判断します。
リアルタイム性が求められる画面ではなく、更新ボタンで十分です。

---

## 5. 着手前に決めること

実装に入る前に、以下を確定させる必要があります。

1. 月別推移の軸は `registration_date` と `delivery_date` のどちらか
2. 編集者別実績にサブ編集者としての参加を含めるか
3. 売上は `estimate_grand_total`（見積全体）と `estimate_total`（編集費のみ）のどちらか
4. 利益系の指標を作るか。作る場合は上記の案A〜Cのどれか
5. 項目別分析をやるか。やるなら `edit_items` と `estimate_items` の統合が先
