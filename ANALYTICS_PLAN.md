# 分析機能 実装計画

**状態: 未実装（計画のみ）**
最終更新: 2026-08-11

---

## 0. このシステムの目的

**従業員の評価制度を明確にするための施策**です。分析機能はその中核にあたります。

見える化したいのは以下です。

- 1つの案件にどれだけの時間を使って編集しているか
- 見積から算出した編集目標時間以内で終えられているか
- 目標と実働の差
- 何件の案件に携わってきたか
- 何稿で案件を終わらせたか
- 使用した技術

**評価に使う数字である以上、集計の正確さと定義の一貫性が最優先です。**
「だいたい合っている」では評価の根拠になりません。
本文中の ⚠️ は、この観点で先に手当てが必要な箇所を指します。

> **旧計画について**
> `編集報告システム_仕様書_完全版 (1).md` の「⑩ ダッシュボード画面」にも
> 分析の記載がありますが、あれは Google Apps Script 時代のもので、
> 売上・利益率が主題であり評価制度の観点がありません。
> 分析機能についてはこの文書を正とします。

---

## 1. 見たい分析（役割別）

### スタッフ（staff / part_time）

| 見たいこと | 指標 |
|---|---|
| 自分がどれくらいできているか | 自分の目標達成率・平均超過時間・案件数・平均稿数 |
| 他の人と比較してどうか | 自分の数字 vs **全体平均・チーム平均**（⚠️ 匿名化する） |

### リーダー・管理職（leader / executive）

| 見たいこと | 指標 |
|---|---|
| 自分がどれくらいできているか | 上と同じ（自分も編集を担当するため） |
| 自分が見ているスタッフはどうか | 配下メンバーの一覧と各指標 |
| 他の人と比較してどうか | リーダー間の比較 |
| 他の人が見ているスタッフはどうか | チーム別の比較 |
| 全体でどうか | 全社の平均・分布 |
| クライアント・案件ごとの所要時間 | クライアント別／案件別の実働時間と目標差 |
| ジャンル別の乖離 | **ジャンル別の目標超過率**（どのジャンルがよく乖離するか） |

---

## 2. 指標の定義

評価に使うため、定義を固定します。**実装前にここを合意してください。**

### 2-1. 基本指標

| 指標 | 定義 | 備考 |
|---|---|---|
| 実働編集時間 | `edit_history.edit_time` の合計（分） | 案件・人・期間で切る |
| 編集目標時間 | `projects.target_hours`（分） | 見積内訳 ÷ 時給 × 60 で算出済み |
| 目標との差 | 実働 − 目標（分） | プラスが超過 |
| 消化率 | 実働 ÷ 目標 × 100（%） | 100%以内が目標内 |
| 目標達成率 | 目標以内に収めた案件数 ÷ 対象案件数 | 人の評価の主軸 |
| 担当案件数 | 対象期間に関わった案件数 | メイン／サブの扱いは下記 |
| 稿数 | 最終的に到達した稿の番号 | ⚠️ 下記の課題あり |

### 2-2. 決める必要がある点

**(a) 「携わった案件」の範囲**

`main_editor` / `sub_editors` / `director` のどれを含めるかで数字が変わります。

推奨は **メイン編集者としての案件を主指標**とし、サブ参加は別指標として分けることです。
サブ参加は作業時間だけ計上され目標時間の責任を負わないため、混ぜると
達成率の意味が壊れます。

**(b) 集計期間の軸**

`registration_date`（登録日）／ `delivery_date`（納品予定日）／
`actual_delivery_date`（実納品日）／ `leader_approved_at`（承認日）のどれか。

推奨は **`leader_approved_at`（承認日）**です。案件が完了した時点で
評価対象に入るため、期首・期末の区切りがぶれません。
ただし承認されていない案件は集計に載らない点に注意が必要です。

**(c) 対象とする案件のステータス**

推奨は **`leader_approved` のみ**。作業途中の案件を混ぜると
「まだ終わっていないから目標内」という誤った評価になります。

**(d) 個人の比較をどこまで見せるか**

⚠️ 評価制度に直結するため、慎重に決めてください。

- スタッフには **他人の個人名を出さず、全体平均・チーム平均との比較**にする
- リーダー以上には個人名を出す

技術的にもこの切り分けが必要です（後述の RLS の項を参照）。

---

## 3. ⚠️ 実装前に手当てが必要なデータ側の課題

**ここを直さずに集計を作ると、評価に使えない数字が出ます。**

### 課題1: 目標時間が見積の変更に追随しません ✅ 対応済み

`target_hours` は**案件登録時にしか計算されていませんでした**。
`saveProject()` では見積内訳から算出していますが、
`updateProject()` に `target_hours` の更新処理がありませんでした。

そのため **案件詳細登録で見積内訳を修正しても、目標時間は登録時のまま**でした。
「目標と実働の差」が評価の主軸である以上、これは致命的です。

**対応（2026-08-11）**

- 見積内訳から合計と目標時間を算出する `calculateEstimateTotals()` を新設し、
  `saveProject()` と `updateProject()` の両方から呼ぶようにしました。
  計算箇所が1つになったため、片方だけ直り忘れることがなくなります。
- `updateProject()` は画面から送られる `estimate_total` ではなく、
  **内訳から計算し直した値を正**として保存します。
- 内訳を全部消した場合に古い行が残っていた不具合もあわせて直しました
  （目標時間0なのに内訳が残る状態を防ぐ）。

- 既存案件のずれは `supabase/migrations/023_recalculate_target_hours.sql` を
  Supabase の SQL Editor で実行して再計算済みです（2026-08-11 適用）。
  同ファイルの STEP 3 のクエリで、不一致が0件であることを再確認できます。

### 課題2: 稿数が数値として保存されていません 🟡

稿数は `project_milestones.milestone_name` という**自由文字列**に
埋め込まれています（例: `初稿` / `3稿` / `最終稿` / `社内確認2` / `社内確認(最終)`）。

現在は `splitMilestoneName()` が文字列を解析して表示していますが、
集計のたびに文字列を解析するのは壊れやすく、表記ゆれにも弱いです。

→ `project_milestones` に稿数の数値カラム（例: `revision_number INTEGER`）と
　 工程種別カラムを追加し、保存時に入れるのが確実です。
　 既存データは `splitMilestoneName()` と同じロジックで一度だけ移行します。

なお「何稿で終わったか」の定義も決める必要があります。
**完了日（`completed_date`）が入っている稿の最大値**が妥当だと考えていますが、
「最終稿」をどう数値化するかは要確認です。

### 課題3: 上長とスタッフの関係が定義されていません 🟡

「自分が面倒を見ているスタッフ」を出すには、誰が誰を見ているかの情報が必要ですが、
現在 `users` にも `teams` にもその関係がありません。

| 案 | 方法 | 長所 | 短所 |
|---|---|---|---|
| A | `teams` に `leader_id` を追加 | チーム単位で明快。組織図と一致 | 兼務や複数チームに弱い |
| B | `users` に `supervisor_id` を追加 | 個人単位で柔軟 | メンテナンスの手間が増える |
| C | `projects.assigned_leader` から実績で導出 | **カラム追加が不要** | 月ごとに顔ぶれが変わり評価軸が安定しない |

→ 評価制度には**安定した名簿**が要るため、**案A**を推奨します。
　 `teams` テーブルは既にあり、`users.team_id` も存在するので、
　 `leader_id` を1つ足すだけで済みます。

### 課題4: ジャンル・技術は配列です 🟢

`projects.genres` / `projects.technologies` は配列のため、
1案件が複数ジャンルに計上されます。**合計が案件総数と一致しません。**
グラフの注釈で明示すれば実用上は問題ありません。

### 課題5: 作業項目マスタが2つに分かれています 🟢

- 見積内訳 `estimate_breakdown.edit_item_id` → **`edit_items`**
- 編集時間 `edit_history.estimate_item` → **`estimate_items`**

同じ概念に別テーブルが使われているため、
**「項目ごとの見積 vs 実績」は現状では突き合わせられません。**
案件単位の合計比較には影響しないので、項目別分析をやる段階まで先送りできます。

---

## 4. RLS（アクセス制御）との関係 ⚠️

**スタッフは自分が関わる案件しか取得できません。**
そのため「他の人と比較」をフロント側の集計だけで実現することは**できません**。

対応は、**集計値だけを返す `SECURITY DEFINER` 関数（RPC）**を用意することです。

```
get_evaluation_summary(期間, 対象範囲)
  → 個票ではなく、平均値・中央値・分布のみを返す
  → 関数内で呼び出し元の権限を確認し、
     staff には匿名化した集計のみ、leader 以上には個人別を返す
```

これにより、
- スタッフは他人の生データに触れずに自分の立ち位置を知れる
- リーダーは配下メンバーの個人別データを見られる

の両立ができます。

> `SECURITY DEFINER` は RLS を迂回します。020 で危険な関数を削除した経緯があるため、
> **関数内で必ず `auth.uid()` から呼び出し元の権限を確認し、
> `anon` からの EXECUTE を REVOKE** してください。
> `migrations/021_admin_reset_password.sql` が実装の雛形になります。

---

## 5. 実装の順序

### フェーズ0: データの土台を直す（最優先）

**ここを飛ばして集計を作っても、出てくる数字が評価に使えません。**

1. ~~`updateProject()` で `target_hours` を再計算（課題1）~~ ✅ 2026-08-11 完了
2. ~~既存案件の `target_hours` を再計算するSQLを流す~~ ✅ 2026-08-11 適用済み
   （`migrations/023_recalculate_target_hours.sql`）
3. `teams.leader_id` を追加し、担当リーダーを設定（課題3）
4. `project_milestones` に稿数の数値カラムを追加し、既存データを移行（課題2）

### フェーズ1: 個人の実績

対象: 全権限（自分の数字）

- 担当案件数（メイン／サブを分けて表示）
- 総編集時間・案件あたりの平均編集時間
- 目標達成率（目標以内に収めた案件の割合）
- 目標との差の一覧（超過の大きい順）
- 平均稿数
- 使用技術の内訳

### フェーズ2: 比較

- スタッフ向け: 自分 vs 全体平均・チーム平均（匿名）
- リーダー向け: 配下メンバー一覧と各指標、メンバー間の比較

RPC の実装はここで必要になります。

### フェーズ3: 案件軸の分析

対象: leader / executive

- クライアント別の平均編集時間・目標超過率
- **ジャンル別の目標超過率**（乖離の大きいジャンルの特定）
- 技術別の平均編集時間
- 案件別の一覧（超過の大きい順）

### フェーズ4: 全体・推移

- 全社サマリー（平均達成率・総編集時間・案件数）
- 月別推移
- チーム別比較

---

## 6. 技術的な検討事項

**グラフライブラリが未導入です**
現在 `index.html` が読み込む外部スクリプトは supabase-js のみです。
フェーズ1〜2は数値とテーブルで成立するため、導入はフェーズ3からで構いません。

**集計をどこで行うか**

| 方式 | 使いどころ |
|---|---|
| フロントで集計 | 自分のデータのみを扱うフェーズ1 |
| RPC（`SECURITY DEFINER`） | 他人との比較が必要なフェーズ2以降。**必須** |

20名・年間数百件の規模であれば、性能上の問題は当面出ません。
RPC を使う理由は速度ではなく**アクセス制御**です。

**自動更新は不要です**
旧計画にあった「5分ごとの自動更新」は、評価用途では意味がありません。
更新ボタンで十分です。

---

## 7. 着手前に決めること

1. 「携わった案件」にサブ編集者・ディレクターとしての参加を含めるか（2-2 a）
2. 集計期間の軸をどれにするか（2-2 b）
3. 対象ステータスを `leader_approved` のみとするか（2-2 c）
4. スタッフに他人の個人名を見せるか（2-2 d）
5. 「何稿で終わったか」の定義（完了日が入った稿の最大値でよいか、最終稿の扱い）
6. 上長とスタッフの関係を案A〜Cのどれで持つか（課題3）

**1〜4 は評価制度そのものの設計に関わります。**
数字の意味が変わるため、運用側で先に合意してください。
